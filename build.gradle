buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.4'
    }
}

// ❌ allprojects block hata diya

task clean(type: Delete) {
    delete rootProject.buildDir
}